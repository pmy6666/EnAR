# Pretrained Models

This directory keeps the expected local layout for pretrained assets.

Model weight files are intentionally ignored by Git. Use the scripts in
`work_scripts/` and the parameter files in `pre_model/LLM/` and
`pre_model/DDIM/` to download them locally.
